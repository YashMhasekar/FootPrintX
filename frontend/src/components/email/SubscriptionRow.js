// Re-exports the shared SubscriptionRow component from the top-level
// components directory. Importing from components/email/ keeps email-specific
// pages tidy without duplicating any code.
export { default } from '../SubscriptionRow.jsx';
