// Re-exports the shared FileCard component from the top-level components
// directory. Importing from components/drive/ keeps Drive-specific pages
// tidy without duplicating any code.
export { default } from '../FileCard.jsx';
